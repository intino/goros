def type(graph)
    package $package.$module+lowerCase.graph;

    import io.intino.magritte.framework.Graph;

    public class $module+firstUpperCase~Graph extends $package.$module+lowerCase.graph.AbstractGraph {

    	public $module+firstUpperCase~Graph(Graph graph) {
    		super(graph);
    	}

    	public $module+firstUpperCase~Graph(io.intino.magritte.framework.Graph graph, $module+firstUpperCase~Graph wrapper) {
    		super(graph, wrapper);
    	}
    }
end